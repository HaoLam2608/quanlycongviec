import React from 'react';
// Reuse the manager's project detail view so admin sees identical details
import ManagerProjectDetail from '../(manager)/project-detail';

export default function AdminProjectDetail() {
    return <ManagerProjectDetail />;
}
