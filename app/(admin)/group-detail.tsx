import React from 'react';
// Reuse the manager's group detail view so admin sees identical details
import ManagerGroupDetail from '../(manager)/group-detail';

export default function AdminGroupDetail() {
    return <ManagerGroupDetail />;
}
